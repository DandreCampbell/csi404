public class ALUTest {
    
    public static void runTest() throws Exception {
        Bit[] test = new Bit[4];
        test[0] = new Bit(false);
        test[1] = new Bit(true);
        test[2] = new Bit(false);
        test[3] = new Bit(false);
/* 
        if(ALU.doOp(test, new Longword(2), new Longword(2))) {
            throw new Exception("doOp() - Failed");
        }
        else {
            System.out.println("doOp() - Success");
        }
*/
        System.out.println();
    }

}
